language: objective-c
osx_image: xcode7.2

xcode_project: NetworkKit.xcodeproj
xcode_scheme: NetworkKitTests

before_install:
    - brew update
    - brew install xctool

script:
  xctool -sdk iphonesimulator -project NetworkKit.xcodeproj -scheme NetworkKitTests clean build test
